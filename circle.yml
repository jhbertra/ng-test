general:
  artifacts:
    - "coverage"

dependencies:
  pre:
    - npm install -g npm && npm install -g angular-cli
  override:
    - npm install

deployment:
  staging:
    branch: master
    commands:
      - ng build
      - scp dist serverpilot@159.203.42.60:~/apps/ng-test/public