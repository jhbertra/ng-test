general:
  artifacts:
    - "coverage"

machine:
  environment:


dependencies:
  pre:
    - npm install -g npm

test:
  post:
    - npm run lint

deployment:
  staging:
    branch: master
    commands:
      - npm run build
      - scp -r . serverpilot@159.203.42.60:~/apps/ng-test/public
        pwd:
          dist