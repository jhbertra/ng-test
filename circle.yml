general:
  artifacts:
    - "coverage"

dependencies:
  pre:
    - npm install -g npm && npm install -g angular-cli
  override:
    - npm install

deployment:
  production:
    branch: master
    commands:
      - ng github-pages:deploy